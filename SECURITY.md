# 安全政策

## 支持范围

| 版本 | 状态 |
| --- | --- |
| 1.2.x | 接收安全修复 |
| 1.0.x - 1.1.x | 接收安全修复 |
| < 1.0 | 不支持 |

## 报告漏洞

请使用 GitHub 的 [Private vulnerability reporting](https://github.com/tuolaji996/windows-ssh-key-deployer/security/advisories/new) 私密报告。不要先创建公开 Issue。

报告建议包含：

- 受影响版本和 Windows/macOS/Debian 环境。
- 可重现步骤、影响和预期结果。
- 可行的缓解或修复建议（如果已知）。

请删除日志中的服务器地址、账户、密码、令牌、私钥和完整公钥内容。为证明问题而上传真实凭据不会加快处理。

维护者会尽快确认报告，在修复发布前请不要公开技术细节。

## 使用者安全提示

- 仅从本仓库的 Releases 下载，并核对随包发布的 SHA-256。
- 主机指纹必须通过独立渠道核对；不一致时立即取消。
- 确认密钥回连成功前，保留一个可用的服务器控制台或 SSH 会话。
- 私钥应只保存在受信任的 Windows 账户或本机 macOS 用户目录中，不要上传到 Issue、聊天、网盘或源码仓库。
